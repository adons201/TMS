server:
  port: ${PORT:8000}
spring:
  application:
    name: tms-front
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      profile: cloudconfig