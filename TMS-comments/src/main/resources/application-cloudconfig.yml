server:
  port: 8001
  forward-headers-strategy: native
spring:
  application:
    name: tms-comment
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      profile: cloudconfig






